# LLM provider selection: "mock" (default, no key required) or "anthropic"
WIZARD_LLM_PROVIDER=mock

# Required only when WIZARD_LLM_PROVIDER=anthropic
ANTHROPIC_API_KEY=

# Optional: override the default Anthropic model
WIZARD_LLM_MODEL=claude-sonnet-4-6
