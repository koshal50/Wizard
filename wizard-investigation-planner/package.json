{
  "name": "wizard-investigation-planner",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Wizard AI Engine — LLM-powered Investigation Planner inside a deterministic Runtime (two-graph architecture).",
  "engines": {
    "node": ">=22.6.0"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "node --experimental-strip-types --test --test-reporter=spec \"tests/**/*.test.ts\"",
    "serve": "node --experimental-strip-types src/http/server.ts"
  },
  "devDependencies": {
    "typescript": "5.9.3",
    "@types/node": "26.1.1"
  }
}
